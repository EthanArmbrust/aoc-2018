#include <iostream>
#include <fstream>
#include <vector>

using namespace std;

struct guard{
    int id;
    vector<pair<int,int>> sleep_times;
}

int main(){

    ifstream infile("input/4.txt");
    string s;
    while(getline(infile, s)){
        sscanf(s.c_str(), "
    }  



    return 0;
}
